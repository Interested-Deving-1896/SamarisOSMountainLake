export const SYSTEM_PANEL_ANIMATIONS = {
  transition: "opacity 160ms var(--air-ease), transform 160ms var(--air-ease), filter 160ms var(--air-ease)",
  openTransform: "translateY(0) scale(1)",
  closedTransform: "translateY(-8px) scale(0.985)"
} as const;
